# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Workspace lockfile lives at the monorepo root only — running `npm install`
# from inside this dir would regenerate one and confuse npm workspaces.
package-lock.json

# Environment files (NEVER commit secrets)
.env
.env.*
!.env.example

# Editor / Claude session local settings
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
.claude/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
